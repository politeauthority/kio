apiVersion: apps/v1
kind: Deployment
metadata:
  name: kio-api
spec:
  template:
    spec:
      containers:
        - name: kio-api
          env:
            - name: MQTT_TOPIC_PREFIX
              value: kio/dev
