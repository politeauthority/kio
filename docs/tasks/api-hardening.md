# Api Lock Down
We need to make sure that all the api endpoints are secured as best as possible.

Every endpoint that can possibly support authentication should be required.

Lets also make sure that we use all best security practices for Fast Api server.

Then make sure we create unit and regression tests where applicable to make sure security hardening is being enforced.
